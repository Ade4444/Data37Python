from Shapes import Shape


class Rectangle:

    def __init__(self, length, width):
        self.width = width
        self.length = length

    def get_perm(self):
        return sum(self.side_lengths) +

    def get_r(self):
        return len(self.side_lengths)


print(p)



    def __init__(self):
        self.shape = 'rectangle'
        self.side_lengths = [3, 4, 3, 4]

        rect = Rectangle()
        rect.compute_perimeter()

